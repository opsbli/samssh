# E006 — 当前实现状态（截至 2026-07-20）

**Type**: known_fact
**Date**: 2026-07-20

## Description

记录当前项目代码的完整实现状态，与现有 .spec/ 设计文档的差距。

## Detail

### 已实现的代码与设计文档的差距

| 领域 | 设计文档状态 | 代码实际状态 |
|------|-------------|------------|
| app_state.puml | 引用 `transfer_state`, `theme_state`, `window_state`, `tab_colors` 等不存在的字段 | 实际 AppState 仅有 `sidebar`, `tabs`, `sessions`, `config` 四个字段 |
| app_state.puml | 未包含 Workspace 实体 | Workspace 已实现为持有 Entity<AppState> 的视图 |
| app_state.puml | 未包含 ProfileDialog | dialog.rs 已实现新建连接对话框 |
| app_state.puml | 未包含 ContextMenu | sidebar_tree.rs 已实现右键菜单 |
| architecture.puml | 未包含 Workspace 组件和 ProfileDialog | 两者已实现 |
| module_deps.md | views/ 列出不存在的文件 (terminal_view.rs, file_manager.rs 等) | 仅 workspace.rs 存在，其余为 TODO |
| module_deps.md | dialog.rs 描述为 "通用对话框" | dialog.rs 实际实现为 "新建/编辑连接对话框" |
| module_deps.md | Date: 2026-07-17 | 需要更新到 2026-07-20 |
| requirements_matrix.md | Code/Test 标记为 "待实现" | 代码已部分实现，61 个测试通过 |
| design-to-requirement.md | 未映射 R010（新建连接对话框） | R010 需要新增 |

### 代码实现统计

- **模块数**: 15 个模块（含空壳）
- **测试**: 61 个测试，全部通过
- **UI 组件**: TitleBar（集成）、SidebarTree（集成）、TabBar（定义但未集成）、ProfileDialog（新建）、ContextMenu（新建）
- **UI 视图**: workspace.rs（主工作区）
- **配置持久化**: 新建/删除 Profile 后自动保存到磁盘

## Impact
- 需要同步 4 个设计文档至当前实现状态
- 需要新增 R010 Requirement
- 需要更新追溯矩阵
